version: "3.9"
services:
  server-dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/data
    volumes:
      - server-dashboard-data:/data
    restart: unless-stopped

volumes:
  server-dashboard-data:
